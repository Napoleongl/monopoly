# ============ Chance cards ===========
draw_card <- function(player_id){
  return(TRUE)
}

# ============ Card types =============
give_money_to_player <- function(.player, .board){
  return(TRUE)
}
get_money_from_all <- function(.player, .board){
  return(TRUE)
}
get_gift <- function(.player, .board){
  return(TRUE)
}
move_to_and_buy_or_pay <- function(.player, .board){
  return(TRUE)
}
move_to_and_steal <- function(.player, .board){
  return(TRUE)
}
get_out_of_prison <- function(.player, .board){
  return(TRUE)
}

# ============ Deck definition ========
create_chance_deck <- function(){
  return(TRUE)
}